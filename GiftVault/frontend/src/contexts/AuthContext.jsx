import{createContext,useContext,useEffect,useState}from'react';import{auth}from'../api/client';
const C=createContext();export function AuthProvider({children}){const[user,setUser]=useState(null);useEffect(()=>{if(localStorage.token)auth.me().then(r=>setUser(r.data.user)).catch(()=>localStorage.removeItem('token'))},[]);const login=async d=>{const r=await auth.login(d);localStorage.setItem('token',r.data.token);setUser(r.data.user)};const logout=()=>{localStorage.removeItem('token');setUser(null)};return <C.Provider value={{user,login,logout,setUser}}>{children}</C.Provider>}export const useAuth=()=>useContext(C);
